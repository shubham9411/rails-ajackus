<!DOCTYPE html>
<html>
  <head>
    <title>RailsAjackus</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class="lang-wrapper">
      <span><%= link_to 'English', root_path(locale: :en) %></span>
      <span><%= link_to 'हिंदी ', root_path(locale: :hi) %></span>
    </div>
    <%= yield %>
  </body>
</html>
